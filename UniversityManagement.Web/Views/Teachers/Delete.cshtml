@model UniversityManagement.Core.Models.Teacher

@{
    ViewData["Title"] = "Delete Teacher";
}

<h1 class="mb-4">🗑️ Delete Teacher</h1>

<div class="alert alert-warning" role="alert">
    <strong>Are you sure you want to delete this teacher?</strong>
</div>

<div class="card mb-4">
    <div class="card-body">
        <p><strong>Name:</strong> @Model.FirstName @Model.LastName</p>
        <p><strong>Email:</strong> @Model.Email</p>
        <p><strong>Department:</strong> @Model.Department</p>
    </div>
</div>

<form method="post" action="@Url.Action("Delete", "Teachers", new { id = Model.Id })" style="display:inline;">
    <button type="submit" class="btn btn-danger">🗑️ Confirm Delete</button>
</form>
<a href="@Url.Action("Index")" class="btn btn-secondary">❌ Cancel</a>
